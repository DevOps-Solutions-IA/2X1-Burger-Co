# Phase 2 command mechanism map

| Capability | File | Symbol | Current implementation | Persistence model | Unique constraint | Transaction boundary | Replay behavior | Conflict behavior | Audit behavior | Authorization | Limitation | Phase 2 action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SOFIA order command | `apps/api/src/modules/sofia/contracts/sofia-contract.adapters.ts` | `BlockedSofiaOrderCreationAdapter.createFromSofiaDraft` | Validates key syntax, draft version/status; always blocks | None | None | Read then audit, no command transaction | Repeats validation and blocked audit | Stale version maps to not confirmable | Blocked event through `AuditService` | Actor context passed by caller; controller RBAC upstream | No claim, lifecycle, result replay or approval binding | Wrap behind secure command core; remain blocked until later phase |
| Draft confirmation | `apps/api/src/modules/sofia/sofia.service.ts` | `confirmDraft` | Updates draft to `CONFIRMED` and audits | `SofiaOrderDraft` | ID only | State update and audit are not proven as one transaction | Already confirmed returns current draft | `updatedAt` used later as version, not CAS at confirm call | Audit follows state transition | Authenticated SOFIA route | Confirmation is not a durable scoped approval | Add explicit approval binding before operational execution |
| Request context | `apps/api/src/modules/audit/audit-context.service.ts` | `AuditContextService` | AsyncLocalStorage context with actor/source/request/release | None | None | Request lifetime only | Not persisted | Rejects actor/source/idempotency replacement in request | Supplies context to audit | Auth actor set by guard/interceptor | Lost after request/restart | Materialize immutable command execution context |
| RBAC | `apps/api/src/common/guards/roles.guard.ts` | `RolesGuard.canActivate` | Role or permission match; denied access audited | `AuditLog` for denial | None | Audit is separate from target mutation | Request is re-evaluated | Unauthorized/forbidden errors | Explicit `RBAC_DENIED` | JWT plus roles/permissions | Approval reuse cannot prove same actor/permission snapshot | Policy service rechecks actor and permission at claim and execution |
| Audit | `apps/api/src/modules/audit/audit.service.ts` | `AuditService.log` | Sanitized structured event, optional transaction client | `AuditLog` | None for idempotency key | Atomic only when caller supplies client | Queryable evidence, no result replay | No duplicate prevention | Actor/source/request/release supported | Inherited context or explicit actor | Audit failure semantics vary by caller | Command audit must be required in claim/execution transaction |
| Governance pause | `apps/api/src/modules/sofia/governance/sofia-governance.service.ts` | `pauseGlobal`, `resumeGlobal` | Persists JSON setting and emits audit | `Setting`, `AuditLog` | `Setting.key` | Two independent writes | Last setting wins | No optimistic version | Audit after setting write | Admin endpoints | In-flight command behavior undefined | Policy check before claim and immediately before side effect |
| Kill switch | Same service | `activateKillSwitch`, `deactivateKillSwitch` | Persists kill state and audit | `Setting`, `AuditLog` | `Setting.key` | Two independent writes | Last setting wins | No lease invalidation | Explicit governance events | Admin endpoints | Does not mark claimed commands rejected/expired | Command policy defines pre-claim and pre-side-effect checks |
| Runtime safety | `apps/api/src/modules/sofia/runtime-safety/sofia-runtime-safety.service.ts` | `evaluate`, `recordBlocked` | Fail-closed decisions; hashed identifiers in blocked audit | `Setting`, `AuditLog` | None | Read decision then later caller action | Re-evaluated per call | First blocker returned | Sanitized blocked evidence | Caller and runtime configuration | Decision not durably bound to payload or release | Persist policy-decision hash and reason with command |
| WhatsApp inbound | `apps/api/src/modules/sofia/sofia-whatsapp.service.ts` | `processInboundWebhook` | Provider identity/hash deduplication | `WhatsappInboundEvent`, `WhatsappMessage` | `providerEventId`; provider+eventHash; message idempotency | Ingestion and later processing use multiple writes | Duplicate ignored or existing message returned | Unique conflict recovered | Multiple audit/runtime events | Provider gate/allowlist plus route guards | Unknown-result processing window; domain-specific | Reuse pattern, not tables, for atomic command claim |
| WhatsApp outbound | Same service | outbound queue/send paths | Unique outbound key, attempts and retry timestamp | `WhatsappOutboundMessage` | `idempotencyKey`, `localMessageId` | Queue state separate from external send | Existing record prevents duplicate enqueue | Status controls approval/retry | Send/blocked audits | Governance, allowlist and approval status | External send can precede final DB confirmation | Outbox/attempt policy required before any future real send |
| Payment webhook | `apps/api/src/modules/sofia/sofia-payment-link.service.ts` | `processPaymentWebhook` | Signature validation, trusted event dedup, transactional recognized-event updates | `PaymentWebhookEvent`, `SofiaPaymentEvent`, delivery order | `PaymentWebhookEvent.eventId` | Recognized valid event uses Prisma transaction | Duplicate returns ignored status | Unique event ID prevents replay | Event and state evidence persisted | Signed webhook; no browser trust | Only provider-event idempotency, not operator command idempotency | Preserve handler; do not route payment mutation through generic LLM logic |
| Waiter synchronization | `apps/api/src/modules/orders/orders.service.ts` | `syncWaiterOrder` | Atomic order create/update and receipt creation | `WaiterOrderSyncReceipt`, `OrderTicket` | `clientMutationId` | One Prisma transaction; audit/alerts follow commit | Returns referenced order | Foreign actor conflict; P2002 reload | Audit is after transaction | Waiter access checks and table ownership | Audit not atomic; result shape not persisted independently | Reuse idempotency semantics through domain adapter |
| Order update | Same service | `update`, `replaceItems` | Optional expected revision with `updateMany` CAS | `OrderTicket` | Primary key | Prisma transaction | Caller retries after reload | Zero updated rows -> 409 | Audit inside transaction in core paths | Controller RBAC plus waiter access | Expected revision optional; revision has mixed technical meaning | Require expected aggregate version in secure command input |
| Checkout | Same service | `checkout` | Sale creation, stock/payment mutation and order closure | Sale/order/cash/stock models, `AuditLog` | Domain constraints | Single Prisma transaction for DB effects | No generic idempotency key | Closed order and cash-session checks | Audit inside transaction | Controller roles | Unknown retry could duplicate without command receipt | Future elevated command must claim before entering checkout |
| Operational alerts | `apps/api/src/modules/orders/orders.service.ts`, `apps/api/src/modules/sofia/alerts/sofia-alerts.service.ts` | alert create/resolve methods | Persisted operational alert plus realtime publication | `OperationalAlert` | None | Usually after domain transaction | Duplicate alerts possible unless caller checks | Status OPEN/ACK/RESOLVED | Separate audit depends on caller | Domain endpoint RBAC | Not a command failure/result store | Emit alerts from terminal command outcomes only |
| Release identity | `apps/api/src/release`, `apps/api/src/modules/health`, audit context | release manifest/readiness | Exact source and migration identity at runtime | Image manifest; `AuditLog.releaseVersion` | Build identity | Outside business transaction | N/A | Readiness fails closed | Release stored on audit | Deployment controls | No command-level immutable release binding | Capture release SHA/build ID when command is received and claimed |

## Dependency decision

The secure command core should depend on neutral Phase 1 contracts and dedicated command persistence ports. It must never depend directly on `PrismaService`, `OrdersService` internals, payment providers or WhatsApp providers. Domain command adapters remain authoritative for their own transactions and return sanitized results to the core.

## Implemented command boundary

`SecureCommandModule` now implements the planned ports under `apps/api/src/modules/secure-command`. `PrismaService` is imported only by `persistence/prisma-command.repository.ts`. The registry enables `SOFIA_INTERNAL_VALIDATE` as a non-operational deterministic handler and explicitly blocks order, WhatsApp, payment, stock, cash, sale, delivery-assignment and customer-auto-response commands.
