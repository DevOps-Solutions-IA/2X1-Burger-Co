# Governance

Phase 4 remains supervised and draft-only. The Phase 3 handoff, consent, governance pause, kill switch and receive-only controls remain authoritative. Commercial responses carry an immutable fact envelope; wording cannot change transactional values.

Bounded AI composition is permitted only through the existing Sofia provider in `dry_run`, `suggest` or `supervised` mode. `disabled`, `auto`, rules-only, provider failure and validator rejection all use safe templates. The composer has no tools, command execution or state mutation access.

Order creation, payment, inventory, cash, sale, kitchen, real WhatsApp send and automatic reply are disabled. No production action was executed.
