# Takeaway checkout

Takeaway maps to the existing `PARA_LLEVAR` / `TAKEAWAY` domain. It requires no address, location, rider or delivery workflow. Delivery fee and quote references are always zero/null.

Allowed payment choices are `ONLINE` and `PAY_AT_PICKUP`; `CASH_ON_DELIVERY` is rejected by domain policy.
