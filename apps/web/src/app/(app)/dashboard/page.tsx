import { OverviewScreen } from '@/features/overview/overview-screen';

// Keep the historical route as an alias while bookmarks and operators migrate.
export default function DashboardPage() {
  return <OverviewScreen />;
}
