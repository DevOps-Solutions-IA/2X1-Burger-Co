# AUDIT-7B reference only. Do not copy over active nginx config until cutover.
#
# V1 active upstreams:
#   api  -> http://api:3000
#   web  -> http://web:3001
#
# V2 proposed upstreams after green stack is attached to the nginx Docker network:
#   api-v2 -> http://api-v2:3000
#   web-v2 -> http://web-v2:3001
#
# Cutover approach for AUDIT-7C:
# 1. Keep current server blocks and certificates.
# 2. Replace only proxy_pass upstream targets from api/web to api-v2/web-v2.
# 3. Run nginx -t.
# 4. Reload nginx, not full stop, if possible.
# 5. Smoke /api/health, /login, /pos, /cash, /waiter/login.
# 6. Roll back by restoring the backed-up V1 nginx config and reloading nginx.
